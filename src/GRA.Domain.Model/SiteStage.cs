namespace GRA.Domain.Model
{
    public enum SiteStage
    {
        Unknown,
        BeforeRegistration,
        RegistrationOpen,
        ProgramOpen,
        ProgramEnded,
        AccessClosed
    }
}
